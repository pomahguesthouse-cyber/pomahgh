export function generateKeywords(area: string) {
  return [
    `penginapan ${area}`,
    `guest house ${area}`,
    `homestay ${area}`,
    `hotel dekat ${area}`,
    `penginapan murah ${area}`,
  ];
}