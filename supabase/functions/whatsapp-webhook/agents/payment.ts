/**
 * Payment utility — keyword detection only.
 * The actual payment conversation is handled by the booking agent.
 * Payment proof (image) is handled by paymentProof.ts.
 * Payment approval (manager YA/TIDAK) is handled by paymentApproval.ts.
 */
